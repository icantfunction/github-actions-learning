{
  "name": "github-actions-learning",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal Node.js project for learning GitHub Actions CI.",
  "scripts": {
    "lint": "eslint .",
    "test": "node --test",
    "test:coverage": "c8 --reporter=text --reporter=lcov npm test",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "c8": "^10.1.3",
    "eslint": "^8.57.1"
  },
  "eslintConfig": {
    "env": {
      "node": true,
      "es2022": true
    },
    "extends": "eslint:recommended"
  }
}
